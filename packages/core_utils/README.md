# core-utils

Shared low-level utilities for packages and apps in this workspace.

`core_utils.files` contains file-system helpers that are intentionally free of dependencies on higher-level packages.
